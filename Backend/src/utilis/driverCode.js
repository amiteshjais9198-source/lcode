/**
 * Wraps the complete code with driver code if necessary.
 * Currently, completeCode is expected to be a fully executable script
 * that reads from stdin and writes to stdout.
 */
function wrapWithDriverCode(language, completeCode) {
    return completeCode;
}

module.exports = wrapWithDriverCode;
